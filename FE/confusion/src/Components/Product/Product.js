import React from "react";
import overviewhome from "../../data/overviewhome.json"; // Path to your JSON file
import { Link } from "react-router-dom";

export default function Product() {
    return (
        <div style={{ height: "100%", overflowY: "auto" }}>
            <div>
                <h1>List</h1>
            </div>
        </div>
    );
}
